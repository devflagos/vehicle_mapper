vehicle_mapper/*.gem
vehicle_mapper/*.rbc
vehicle_mapper/.config
vehicle_mapper/coverage/
vehicle_mapper/InstalledFiles
vehicle_mapper/pkg/
vehicle_mapper/spec/reports/
vehicle_mapper/spec/examples.txt
vehicle_mapper/test/tmp/
vehicle_mapper/test/version_tmp/
vehicle_mapper/tmp/**/**

# Used by dotenv library to load environment variables.
# .env

# Ignore Byebug command history file.
vehicle_mapper/.byebug_history

## Environment normalization:
vehicle_mapper/.bundle/
vehicle_mapper/vendor/bundle
vehicle_mapper/lib/bundler/man/

# for a library or gem, you might want to ignore these files since the code is
# intended to run in multiple environments; otherwise, check them in:
# Gemfile.lock
# .ruby-version
# .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
vehicle_mapper/.rvmrc

# Used by RuboCop. Remote config files pulled in from inherit_from directive.
# .rubocop-https?--*
